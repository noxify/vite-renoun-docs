export const metadata = {
  title: "shellscript Grammar",
}

## shellscript

```shellscript
VAR_NAME="value"
```

## shell

```shell
VAR_NAME="value"
```

## sh

```sh
VAR_NAME="value"
```

## bash

```bash
VAR_NAME="value"
```

## zsh

```sh
users=("Alice" "Bob")
for user in "${users[@]}"; do
  if [ "$user" == "Alice" ]; then
    echo "Hello, $user (admin)"
  else
    echo "Hello, $user"
  fi
done
```
